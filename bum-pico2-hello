#!/bin/bash
set -e
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
APP_UF2="$REPO_ROOT/platform/pico/build-pico2-hello/commander.uf2"

cd "$REPO_ROOT/platform/pico"
cmake -B build-pico2-hello -S . -DPICO_BOARD=pico2_w -DCOMMANDER_APP=uart_hello
cmake --build build-pico2-hello -j4

enter_bootsel() {
    PORT=$(python3 "$REPO_ROOT/scripts/find_port.py" pico2 2>/dev/null || true)
    if [ -n "$PORT" ]; then
        python3 - "$PORT" <<'EOF'
import sys, serial, time
with serial.Serial(sys.argv[1], 115200, timeout=2) as s:
    time.sleep(0.1); s.write(b'bootloader\r\n'); time.sleep(0.5)
EOF
        for i in $(seq 1 20); do picotool info &>/dev/null && return 0; sleep 0.5; done
        echo "error: device did not enter BOOTSEL" >&2; exit 1
    else
        echo "No running device found. Hold BOOTSEL while plugging in, then press Enter."
        read -r
    fi
}

picotool info &>/dev/null || enter_bootsel
picotool load -f "$APP_UF2"
picotool reboot
echo "Done — Pico 2W sending hello world on UART1 GP4(TX)/GP5(RX) at 115200"
